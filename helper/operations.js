export const sumar = (a, b) => Number(a) + Number(b);

export const restar = (a, b) => Number(a) - Number(b);

export const multiplicar = (a, b) => Number(a) * Number(b);

export const dividir = (a, b) => (b === 0 ? 0 : Number(a) / Number(b));
